[project]
name = "research-topic-validation-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.115.6",
  "httpx==0.28.1",
  "pydantic==2.10.4",
  "uvicorn[standard]==0.34.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
